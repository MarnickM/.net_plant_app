<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="plantdration.Views.UserListView"
		     xmlns:models="clr-namespace:plantdration.Models"
             
             xmlns:viewModels="clr-namespace:plantdration.ViewModels"
			 x:DataType="viewModels:IUserListViewModel"
             
			 Title="Users">

    <!-- Add an item to the toolbar -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddUserCommand}" IconImageSource="{FontImage Glyph='+', Color=Black, Size=22}" />
    </ContentPage.ToolbarItems>

    <!-- Display users in a list -->
    <CollectionView ItemsSource="{Binding Users}"
                    Margin="20"
					SelectionMode="Single"
					SelectedItem="{Binding SelectedUser}"
                    SelectionChangedCommand="{Binding UpdateUserCommand}">

        <!-- Designate how the collection of items are laid out -->
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
        </CollectionView.ItemsLayout>

        <!-- Define the appearance of each item in the list -->
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:User">
                <StackLayout>
                    <Label Text="{Binding Name}" FontSize="22"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
